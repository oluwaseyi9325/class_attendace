// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["via.placeholder.com", "randomuser.me", "png.pngtree.com", "i.pinimg.com", "api.qrserver.com"],
  },
};

export default nextConfig;
